# 04 · W5：Cron、内置 Skill、错误面板、存储收口

对应 Issue #20 #21 #22 #23。上游：PRD FR11/FR12（内置）/FR14、9.3、10、11.3、12.1 G03/G08/G19/G20，12.2 N07–N09/N16/N17；`00-foundation.md` §4/§5；`02-w3` §1；`03-w4` §2（注册表）。

## 0. 原则

- Cron 由 **Jones daemon 自己调度**（`crons` 表已在 schema v1；daemon 是常驻进程，PRD 11.2「空闲不轮询、用系统定时器唤醒」）。Hermes 自带的 `cronjob_manage` 工具与 `cron/` 依赖 Hermes gateway 进程，与 Jones 的进程模型不符——在 worker toolsets 里**禁用** `cronjob_manage`（H 的 toolsets 下发已排除 `kanban_*`/`ha_*`/`computer_use`/`delegate_task`，这里加一项），不重复造两套 cron。
- 内置 Skill 尽量**复用 Hermes 已有工具集**：媒体生成 = Hermes `image_gen`/`video_gen`/`tts` toolsets + 一个 Jones 内置 Skill 做编排说明；办公文档 = 一个 Jones 内置 Skill（说明 + 随 Skill 分发的 Python 脚本，依赖固定）。不在 daemon 里写文档渲染代码。
- 错误面板是「诚实失败」的落地：daemon 负责**分类**并给结构化卡片，renderer 只渲染，不猜。

## 1. 分工与文件所有权

| 分支 | Issue | 独占 | 允许的共享改动 |
|---|---|---|---|
| L `w5/20-cron` | #20 | `daemon/src/jones_daemon/scheduler/`（新）、`daemon/tests/test_scheduler*.py` | 只通过 `SessionService` 公开方法（`create`/`send`/`get`）派发，不改 sessions 内部；`__main__` 加一行 |
| M `w5/21-bundled-skills` | #21 | `daemon/src/jones_daemon/skills/bundled/**`（新）、`daemon/tests/test_bundled_skills*.py`、`tests/integration/` 对应用例 | `skills/service.py`：只把 bundled 目录接进第三层（K 已留位）；toolsets 下发：在 H 的清单里**允许** `image_gen`/`video_gen`/`tts`（改 `capabilities/` 的 toolsets 常量一处，报告标明） |
| N `w5/22-error-panel` | #22 | `daemon/src/jones_daemon/errors/`（新：分类器 + 卡片构造）、`apps/desktop/src/renderer/**` 中的错误/终止卡片与重试动作、`daemon/tests/test_errors*.py` | `sessions/service.py`：`_terminate_run`、`_on_worker_crash`、新增 `retry`（公开方法）；**控制者裁定 R-N4/R-N5（2026-09-20）新增授权**：`_advance_queue`、`_run_turn`、`_handle_tool_call_start` 三个函数（见 §4.3），新增 `queue_resume`（公开方法）；`_run_turn` 追加一行——解析 `model_pref` 前先查 `self._turn_model_override.pop(turn_id, None)`——这是"`model_override` 写入本 Turn 的 provider 解析"唯一可能的接线点；`sessions/methods.py` 加 `session.retry`/`session.queue_resume`；`kernel/acp_client.py`：只加错误分类所需的异常类型信息，不改协议；`config/resolver.py::DEFAULT_SETTINGS`：只加 `max_steps_per_run`/`max_run_duration_s` 两个键（01-w2-interfaces.md §4.1，R-N5 明确要求写入这一节，实现该结构需要这一处最小改动）。**控制者裁定（第 6 轮，2026-09-20）新增授权**：`_terminate_run_for_exceeded_budget`/`_on_worker_crash`/`_run_turn`/看门狗相关代码（新方法 `_run_budget_watchdog_loop`/`_check_run_durations`）、`sessions/queries.py`（新函数 `set_queue_suspended_reason`）、新增迁移 `008_queue_suspended_reason.sql`、`queue_resume()`/`retry()` 的 abandon 分支、`queue()` 的返回形状，以及 renderer 队列面板相关状态与 `chatStore.ts`（见 §4.4——`QueuePanel.tsx` 本身通过 props 消费 `chatStore` 已有的 `suspendedReason` 状态，这轮实际未改动该文件） |
| O `w5/23-storage` | #23 | `daemon/src/jones_daemon/store/maintenance.py`（新：真删、备份轮转、迁移校验）、`daemon/src/jones_daemon/store/methods.py`（新：`daemon.clear_cache`）、`daemon/tests/test_storage*.py`、`docs/design/00-foundation.md` §4.1/§6 追加 | `sessions/methods.py` 加 `session.delete`/`session.export`/`run.delete`；`replay/store.py::purge_run` 调用（未改该文件本身，只是调用方）；`secrets/vault.py` 原子写 fsync + `VaultKeyMismatchError`；`paths.py` 只加访问器（`project_attachments_dir`）；`projects/service.py::delete`（改为调 `maintenance.delete_project`，落实 §5 原文的「project.delete（已有，改为调 maintenance）」——原表格此格未列出该文件，实现时按 §5 正文补上，见 O 分支报告「契约变更」）；`store/migrator.py::_backup_before_migrating`（迁移备份保留最近 5 份，同一理由补列）；`rpc/server.py`（仅新增 `broadcast_all`/响应采样环形缓冲，"加法不改法"，该文件本就允许任何 W2 分支这样扩展）；`__main__.py` 常规「加几行」（注册 `store/methods.py`、启动期 Key 脱敏自检、日志滚动后台循环） |

## 2. L：Cron（FR11）

- `scheduler/cron_expr.py`：五段 cron 解析 + `next_after(dt)`（自己写，≤150 行，有边界测试；不引第三方）。
- `scheduler/service.py`：启动时加载 `crons` 表 `enabled=1` 的项，计算 `next_run_at`；**单个** `asyncio` 定时器等待最近一项（不轮询）；到点 → 若已有该 cron 的 Run 在跑则跳过并记 `skipped_overlap`；否则 `SessionService.create(project_id, agent_id, parent_id=<主会话>, mode=cron.mode 默认 auto，system_dispatch=True，见 §2.2, title=cron 名)` → `send(prompt)`；`tasks` 行 `source=cron`。
- **结果推回主会话**：Run 结束（成功/终止）后向主会话插入一条 `role=system` 消息（摘要 + 子会话链接 + 终止卡片若有），并广播 `message.completed`。
- **失败计数**：连续失败 3 次 → `enabled=0` + 主会话系统消息「已自动停用」（PRD 12.3 FR11）。
- **时钟与恢复**：daemon 启动时对错过的触发**不补跑**（PRD 5.8 精神；记录一条 `missed` 日志与主会话提示）；`runtime/` 里记 `next_run_at` 快照（`runtime/cron_schedule.json`，best-effort 镜像——`crons.next_run_at` 才是 `_loop` 实际调度依据的事实源，这份文件写失败只记日志，不影响派发）。
- RPC `cron.list/upsert/delete/run_now` 已在 v0；`run_now` 走同一派发路径。
- 测试：可注入时钟；分钟精度；重叠跳过；三次失败停用；Electron 无关（纯 daemon 测试即可证明）。

### 2.1 第 1 轮评审后追加的契约决策（round-1，2026-09-19）

- **`cron.upsert` 的 `mode` 默认值一度改为 `task`（round-1，已被 round-3 取代，见 §2.2）**：`ensure_main_session()`（`sessions/service.py`，不在 L 范围）恒把主会话建成 `mode=task`；`SessionService.create()` 的 PRD 9.6/N13 闸拒绝 `mode=task` 父会话下的 `mode=auto` 子会话。这两条都是既有、正确的 PRD 落地，但同时成立时，PRD 9.1「Cron 触发的 Run 默认以自动模式运行」这句话在当前系统里**无法达成**——默认配置下第一次 `create()` 就会被拒。这是 PRD 内部两条要求的真实冲突，不是本分支能单方面通过设计文档改写 PRD 的地方；在跨分支裁定之前，L 在自己独占的 `scheduler/` 范围内选择**不让默认配置开箱即挂**：`cron.upsert` 不传 `mode` 时用 `task`。这一段作为 round-1 决策的历史记录保留；§2.2 记录了后续跨分支裁定如何取代它。
- **cron 表达式按本机系统时区解释，存储仍是 UTC ISO**：`scheduler/cron_expr.py` 的 `next_after` 本身不关心时区（按调用者传入的 `datetime` 的 tzinfo 计算）；`scheduler/service.py::_next_after_local` 是唯一做 UTC↔本地转换的地方——把 `Clock.now()`（UTC）转成本机时区再求下一次匹配，再转回 UTC 存库。理由：这是单机桌面调度器，用户写 `"0 9 * * *"`的直觉预期是"本机每天早 9 点"，不是 UTC 9 点；`00-foundation.md` §4.1 规定的是**存储格式**（UTC ISO），不等于"表达式按 UTC 解释"，此前的实现把这两件事混为一谈。已知限制：本机时区偏移若不是整小时（极少数地区，如 UTC+5:30），跨时区部署会有分钟级别的边界效应；v1 不为此加 `tz` 列，桌面单机场景下按需再收口。

### 2.2 第 3 轮：跨分支裁定收口 mode 冲突（round-3，controller，2026-09-19）

- **裁定**：§2.1 遗留的跨分支缺口——「(a) 主会话默认改 `auto`」「(b) 给系统派发开豁免口」「(c) cron 默认 `mode=task`」——按 (b) 收口。`SessionService.create()`（`sessions/service.py`）新增关键字参数 `system_dispatch: bool = False`：为 `True` 时跳过 PRD 9.6/N13 的父模式收窄校验（`parent.mode=="task" and mode=="auto"` 那一支），仅 `CronService._dispatch_body_claimed` 会传 `True`；RPC `session.create`（`sessions/methods.py`）不读这个字段，外部调用者永远拿到 `False`，不构成权限口子。理由：9.6/N13 的闸是防「Agent 自我提权」，而 cron 的 `mode` 是用户在 `cron.upsert` 时自己显式配置、prior 授权好的，不是 Agent 在会话里现场选的——把两种「谁在决定 mode」的场景用同一道闸拦，本身就是过度收紧。N13 的另一半（工具白名单 ⊆ 父会话）不受影响，继续由 `permissions/gate_config.py` 对活的父链持续强制。
- **随裁定收口**：`cron.upsert`（`scheduler/service.py`、`scheduler/methods.py`）的 `mode` 默认值改回 PRD 9.1 字面的 `"auto"`——round-1 选 `task` 只是因为当时这条路径过不了 9.6 闸，纯属临时避让；闸本身现在已经对 cron 派发开了口子，没有理由再让默认值绕开 PRD 明文要求的「开箱自动模式」。用户仍可在 cron 定义里显式设 `mode="task"`（PRD 9.1「用户可在 Cron 定义里改为任务模式」）。
- **验证**：`daemon/tests/test_scheduler_service.py` 用真实、未改动过校验逻辑之外的 `SessionService`（非 stub）分别验证「默认 `mode`（现为 `auto`）对真实 9.6 闸能派发成功」与「显式 `mode=auto` 同样成功」，两者 `fail_count` 都为 0，不再产生 `cron_dispatch_error`。

## 3. M：内置 Skill（FR12 内置）

- 目录：`jones_daemon/skills/bundled/<name>/`，格式与 Hermes 原生 Skill 一致（K 已验证外部目录接法）。
- `office-docs`：SKILL 说明 + `scripts/`（Markdown → Docx（python-docx）、→ PDF（用系统可用路径：优先 `pandoc`/LibreOffice 若存在，否则 reportlab 简版；报告写清选择）、→ 表格（openpyxl）；输出到当前 Project 目录，路径由 agent 传入）。依赖以 `uv run --with` 内联脚本元数据（PEP 723）声明，**不加进 daemon 的 pyproject**。
- `media-gen`：说明如何用 Hermes `image_generate` / `text_to_speech` / video 工具；需要哪个厂商 Key 走 B 的 provider（报告列清变量名）；没有 Key → provider_error 卡片。
- 验收（12.3 FR12）：办公文档四种格式各产出一份可打开（用 python 打开校验：docx 用 python-docx 读回、pdf 检查 %PDF 头 + 页数、xlsx 用 openpyxl 读回）；媒体三种各一份可播放（检查文件头/时长）——需要模型 Key 的部分 `JONES_E2E` 门控，报告如实写。

## 4. N：错误面板（FR14）

- `errors/classify.py`：把 daemon 能遇到的失败归成固定枚举 `ErrorKind = network | provider_auth | provider_quota | provider_error | tool_exception | worker_crash | approval_timeout | budget | internal`；输入是异常对象 / ACP 错误 / 子进程退出码，输出 `ErrorCard {kind, title, message, step_seq?, raw_excerpt(≤2KB, 已脱敏), actions:[retry|switch_model|abandon], retryable: bool}`。Key 脱敏走 B 的现有工具函数。
- `_terminate_run(kind="error"|"budget", card)`：`run.terminated` 的 `card` 字段统一用 ErrorCard；预算终止（token / API 额度 / 11.2 的 Step、时长上限）也走它。
- **N07**：worker 存活由 `WorkerManager._watch_exit` 已知；再加 daemon 侧「运行中 Run 超过 5s 没有任何 ACP update 且 worker 进程已退出」→ 立即 `worker_crash` 终止；UI 的「运行中」指示器只信 `turn.started`/`run.terminated`，不自己猜。
- RPC `session.retry {id, turn_id?, model_override?}`：重新发起该 Turn（新 Turn，用户消息复用），`model_override` 写入本 Turn 的 provider 解析；`abandon` = 清掉队列中该 Turn 的后续（`queue_items`）并标记 Run。
- RPC `session.queue_resume {id}`（R-N4，见 §4.3）：终止（三类之一）后挂起的队列，用户点「继续」弹出并启动下一条排队指令。
- renderer：错误卡片三个动作接 RPC；网络/配额/认证/工具/崩溃/超时/预算 7 类卡片样式区分（颜色 + 图标），每张卡片有「查看原始错误」折叠。
- 测试（G08 四种故障注入，全用假 ACP agent / provider stub，不联网）：断网（provider 解析抛 network）、Key 失效（provider_auth）、工具抛异常（ACP tool_call 返回 error）、kill worker（`worker_crash` ≤ 5s 被发现）。每种断言：出卡片、进程不崩、`run.terminated` 载荷形状正确、UI 无白屏（vitest 渲染卡片）。

### 4.1 落地时的契约细化（2026-09-19，实现阶段发现，非设计推测）

- **脱敏没有「B 的现有工具函数」可复用**：核对过 `secrets/vault.py`、`providers/methods.py` 后，`_key_hint`（末 4 位）是模块私有、只给"已经拿到手的一个 key 算 hint"，不是"在任意文本里找并遮蔽 key"的通用函数；`DaemonContext` 也没有 `vault` 字段，`_terminate_run` 拿不到"当前配置了哪些 key 的明文"去做精确匹配。`errors/classify.py::redact_secrets()` 因此是本分支新写的、基于**模式**（`sk-` 前缀、`key=`/`token=`/`Bearer ` 后跟长不透明串、`AKIA` 前缀、`XXX_API_KEY=value` 环境变量赋值）而非精确匹配的遮蔽——比"仅遮蔽已知配置的 key"更保守（连未配置厂商泄露的 key 文本也能盖住），代价是启发式，見该文件模块 docstring 与 `test_errors_classify.py` 的已知假阴性/假阳性说明。
- **`run.terminated` 新增 `turn_id`**：00-foundation.md §4.2 原契约没有它，`session.retry` 定位"重试哪个 Turn"必须靠它，`_terminate_run`（本分支唯一改动点）顺带补上，已同步该文档。
- **`ErrorKind` 的 9 类 → 渲染的 7 类视觉样式**：`provider_error`/`internal` 没有独立颜色图标，回退成通用样式——`classify.py::VISUALLY_DISTINCT_KINDS` 是这个映射的唯一事实源，`TerminationCard.tsx` 的 `KIND_STYLE` 与之对应。
- **`kind="budget"` 的两种来源**：① 调用方显式传 `kind="budget"`（今天没有任何调用点这样做，见下"没做什么"）；② `_terminate_run` 从 `reason` 文本分类出 `ErrorKind.PROVIDER_QUOTA`（限流/额度用尽）时，即使调用方传的是 `kind="error"`，`classify.terminated_kind_for()` 也会把最终广播的 `run.terminated.kind` 升级成 `"budget"`——PRD 9.3 原文把"远端 API Key 额度受限/被限流"明确归在预算终止，不是错误终止。
- **`session.retry` 的「换模型」不是独立 action**：`{action:"retry", model_override}` 就是"换模型再试一次"，没有单独的 `action="switch_model"`——两个名字指向同一个操作，见 `SessionService.retry()` 的 docstring。
- **`retry()` 的 model_override 落地**：见 §1 表格 N 行新追加的 `_run_turn` 一行改动——`self._turn_model_override`（新增实例状态，`__init__` 里初始化，键为新 Turn 的 turn_id，`retry()` 写入、`_run_turn` 读出即弹出）。
- **`abandon` 复用 `cancelled`，没有发明新状态**：`queries.cancel_turn`（`queue_remove` 已在用的同一个状态值）用来标记被放弃的 Turn 和排队中每条被清掉的 Turn，没有新引入 `turns.status='abandoned'` 这类只有本分支用的新词汇。
- **`_terminate_run` 的幂等保护（round-N2 controller 裁定 R-N1 收紧，round-N2 复审 #3 再收紧一次）**：写库前先读一次 `runs.status`，非 `'running'` 时不再无条件跳过——`_on_worker_crash` 的 N07 兜底和"真的 `prompt()` 自己发现"两条路径理论上可能在极窄的窗口内都想终止同一个 Run，其中一条可能在 `mark_run_terminated` 的 DB 写已经落地、但自己的 `run.terminated` 广播还没发出去之前被 `task.cancel()` 打断（`store/db.py` 单线程 `ThreadPoolExecutor`，取消 asyncio 层的 await 不会停掉已经在 DB 线程上跑着的 UPDATE）。`SessionService` 因此额外维护一个内存 `set[run_id]`，只记录"广播过"（不是"写过"）——幂等守卫读到非 `running` 时，先查这个 run_id 是否已经广播过：广播过，才是真无操作。**`runs.status` 非 `running` 有两种值，不只 `'terminated'` 一种**：`mark_run_completed`（正常成功结束）也会把它翻成 `'completed'`，而成功完成的 Run 从不广播 `run.terminated`，自然也从不在这个 set 里——最初实现只判 `status != 'running'` 就误把每一个"看门狗晚到一步、Run 其实已经正常 completed"的窗口也当成"写过没广播"，从值全是 `NULL` 的 `terminated_kind`/`terminated_reason` 伪造出一张 `kind=None` 的"内部错误"终止卡，糊到一条成功结束的对话上。收紧后：只有 `status == 'terminated'` 且 `terminated_kind` 非空时才进入"没广播过就补发"分支（从持久化的 `runs` 行重建 `ErrorCard` 并补发一次 `run.terminated`）；`'completed'`（以及理论上不该出现的、`terminated` 但缺 `terminated_kind` 的行）维持原本"debug 日志 + 直接 return"的无操作，`_rebroadcast_terminated_run` 自身也对 `terminated_kind` 为空做一次防御性早退。保证的是"每个真正终止过的 Run 至少广播一次"，不是"每个非 running 的 Run 都要广播一次"。

### 4.2 没做什么（契约要求但本分支未接线）

- ~~**11.2 的 Step 数 / 时长上限触发预算终止**~~ — **已由控制者裁定 R-N5（2026-09-20）接线，见 §4.3**。原文保留供审计：需要在 `_run_turn`/`_handle_tool_call_start` 里主动数 Step、算耗时并在触顶时调用 `_terminate_run(kind="budget", ...)`——这两个函数原本是 A/#10 的专属函数，04-w5-interfaces.md §1 没有把它们开放给 N；本分支只让 `_terminate_run` 在**收到** `kind="budget"` 时能正确出卡，没有新增任何调用点去触发它。同理 token 预算（Goal 层面）依赖 FR17（P1，仍未实现，不在 R-N5 范围内）。round-2 评审裁定：这不能停在"分支报告的没做什么"里——按 `05-w6-interfaces.md` §0 的规则本应开一个 `[验收缺口]` Issue，但本轮修复执行时 `gh issue create` 被 Claude Code 的 auto-mode 分类器拦截（副作用外部操作，未经人工批准）；命令与文案已写进本分支报告"round-2 修复记录"的 open 列表，留给控制者/用户手动执行或裁定归属分支——**这一步本身仍未执行**，与 Step/时长上限的代码接线是两件独立的事。
- **「换模型」（`retry(model_override=...)`）不再是卡片上的可点操作（round-2 已收口，非"已知限制"待定）**：round-1 记录的限制原样成立——`_run_turn` 读出 `self._turn_model_override.pop(turn_id, None)` 后唯一用途是 `ctx.providers.resolve(model_pref)` 这个**预检**，真正 spawn worker 用的是 `workers/manager.py::_spawn_and_check` 调 `_worker_env(hermes_home)`（不带 `extra_env`），且 `WorkerManager.ensure_started` 对已有 worker 的 session 直接复用、不重启——worker 子进程实际使用的 provider/model 绑定从未换成用户选的那个。round-2 评审给出两条路：控制者裁定打开 spawn 路径把 `ProviderBinding` 接进去（含换模型强制重启该 session 的 worker，A/#10 territory），或者在接线之前把 `switch_model` 从 `_ACTIONS` 摘掉、给诚实文案。本轮按第二条路收口（在 N/#22 自己的授权目录内可完成，不越权）：`errors/classify.py::_ACTIONS` 不再对任何 `ErrorKind` 提供 `switch_model`（此前只有 `PROVIDER_AUTH`/`PROVIDER_QUOTA`/`BUDGET` 三类"唯一操作"最致命，但 `switch_model` 对其余类型同样是无效点击，round-2 一并摘除，不留半个假操作）；`provider_auth`/`provider_quota`/`budget` 三类卡片现在只剩 `abandon`，`build_card()` 给这三类的 `message` 追加"去设置页换 Key / 换默认模型后重发"提示。`session.retry` 的 `model_override` **RPC 参数本身保留**（`_run_turn` 仍读取、仍走预检）——只撤回 UI 侧的按钮，一旦未来某分支把 `ProviderBinding` 接进 spawn 路径，恢复只需要把 `switch_model` 加回 `_ACTIONS` 一行。
- **预算详情字段（PRD 9.3"是哪个预算、用了多少、上限多少"）**：round-2 评审 #4 指出统一后的 `ErrorCard` 丢了这三个数字且新形状里无处补。已在 `ErrorCard`/`TerminationCard`（TS）加可选 `budget: {name, used, limit, unit} | None` 字段，渲染器已知道怎么显示——但**没有任何调用点往里填真实数字**：11.2 的 Step/时长触发点本身不存在（见上一条），provider 的 429/quota 文本也不是结构化数据，硬解析数字风险高于不填。这是结构性占位，不是"以后再说"——字段已经在，接线仍等上一条的裁定。
- ~~**`_advance_queue` 对错误终止照样立即续跑**~~ — **已由控制者裁定 R-N4（2026-09-20）按选项 (a) 解决，见 §4.3**。原文保留供审计：`_run_turn` 的 `finally: await self._advance_queue(session_id)` 曾经无差别调用——不管这个 Run 是成功、用户停止还是错误/预算终止，队列里排队的下一条指令立刻 `pop_next_queue_item` + `_start_turn`，早于错误卡片广播后用户能点"放弃"的时间点。留给控制者裁定的两条路是：(a) `_advance_queue` 对 `kind != "user"` 的终止不自动续跑，或 (b) PRD 9.3"终止不清空 Session 队列；队列中的后续指令挂起"这句话改掉，接受"错误/预算终止后队列仍自动续跑"。**R-N4 选了 (a)，且把范围从"`kind != "user"`"扩大到全部三类（含用户手动停止）**——PRD 9.3 的"终止不清空队列；队列中的后续指令挂起"这句话本来就在"终止的通用规则"一节，没有把用户终止排除在外，(a) 选项草案里的"`kind != "user"`"是分支报告写报告时的保守简化，不是 PRD 原文的限定。
- 见报告"round-2 修复记录"获取完整清单（含 N07 watchdog 竞态的进一步收口）。

### 4.3 第 5 轮契约变更（controller ruling R-N4/R-N5，2026-09-20）

- **R-N4：队列挂起（PRD 9.3）**：`_run_turn` 的 `finally` 只在 Turn 正常完成（`_finalize_turn_success` 的 `mark_run_completed` 路径，从不设置下面这个标记）时才让 `_advance_queue` 弹出并启动下一条排队指令；**任何** `_terminate_run` 调用（`kind` = `user`/`error`/`budget` 三者之一）后，队列不再自动续跑——`_terminate_run` 在写库前，把这次终止的 outer `kind`（"user"/"error"/"budget"）同步写进 `ctx_turn.terminated_kind`（`_TurnContext` 新字段，计算完 `effective_kind` 到这次赋值之间没有 `await`，因此不会被跨协程的 `task.cancel()` 打断），`_advance_queue` 从刚结束的 `ctx_turn` 上读它来决定"续跑"还是"挂起"——不需要为此引入任何新的跨 session 持久状态：一个 Session 的队列"是否挂起"完全由"队列里还有没有待发条目 + 当前有没有 Run 在跑"这两件既有事实推出，`queue.changed` 广播在挂起的那一刻额外带 `suspended:true, reason:<kind>`，其它未改动的广播点（`queue()`/`queue_remove()`/`queue_reorder()`/`send()` 入队分支）不带这两个字段，渲染端把缺失当 `suspended:false`。
  - **两种显式恢复动作**：① 新 RPC `session.queue_resume {id}`（"继续"按钮）——弹出并启动下一条排队指令，会话当前有 Run 在跑或队列本就没有待发条目时拒绝（`INVALID_STATE`，参照 `retry()` 已有的"重复动作即拒绝，不静默 no-op"先例）。② `session.send` 一条新消息——`send()` 本身未改动：既然挂起后该 Session 已经"没有 Run 在跑"（`_active_turns`/`_turn_tasks` 在挂起时已被 `_advance_queue` 清理），`send()` 现有的"未运行 -> 立即执行"分支天然让新消息立刻开始跑（跳到原队列前面，不是插到队尾——这是"发消息即恢复"读法下可接受的语义，写在 `_advance_queue` 的实现注释里），新 Turn 正常结束后，`_advance_queue` 按同一套逻辑自然弹出原来挂起的那些条目——旧队列没有被丢弃，只是没被新消息抢在前面。
  - **`abandon` 清空该 Session 队列**：`retry(action="abandon")` 本来就会清空 `queue_items` 里所有待发条目并把它们的 Turn 标 `cancelled`（round-1 已实现，未改动）——清空之后自然没有"待续跑"的条目，不需要额外清理任何挂起标记（因为本次改动没有引入独立于"队列是否有待发条目"的挂起标记）。
  - ~~**已知窄竞态，本轮未修复，留待控制者/未来分支裁定**：N07 崩溃看门狗……~~ —— **已由控制者裁定 R-N10（第 6 轮，2026-09-20）关闭**，见 §4.4：`_on_worker_crash` 本轮起也在 N 的授权改动点内，`task.cancel()` 已挪到 `_finalize_streamed_messages`/`_terminate_run` **之后**，与 `_terminate_run_for_exceeded_budget`（R-N7）同一顺序。原文保留供审计：N07 崩溃看门狗自己的 `task.cancel()` 曾经发生在它自己的 `_terminate_run` 调用**之前**（不是之后）——如果被取消的 `_run_turn` 任务在看门狗自己让出控制权（它下一次 `await`）期间抢先跑到 `finally: await self._advance_queue(...)`，`_advance_queue` 读到的 `ctx_turn.terminated_kind` 可能还是 `None`（看门狗的 `_terminate_run` 还没来得及把它写上），这一次特定的交叉调度下，worker_crash 触发的终止可能不会挂起队列（正常续跑）。
- **R-N5：11.2 Step / 时长上限（PRD 11.2、9.3）**：`settings.json` 新增两个键（01-w2-interfaces.md §4.1 已同步）：`max_steps_per_run`（默认 200）、`max_run_duration_s`（默认 7200，2 小时）——与 PRD 11.2 表格数字一致。`_run_turn` 在拿到 Session 之后、spawn worker 之前，解析一次这两个键（走既有的 `ctx.config.settings(project_id)`，与 `approval_timeout_minutes` 同一次合并结果），连同 `started_at`（`_TurnContext` 构造时刻）一起存在这次 Turn 自己的 `_TurnContext` 上，不逐次调用重新读——`_handle_tool_call_start` 每次 ACP `tool_call` "started" 事件到达时用这份缓存值做一次纯内存比较：Step 计数（`step_seq+1 > max_steps_per_run`）或累计时长（`monotonic() - started_at > max_run_duration_s`）任一超限，即调用 `_terminate_run(kind="budget", ..., budget={name, used, limit, unit})`（`ErrorCard.budget` 字段首次被真实填充，不再永远是 `None`——见 `errors/classify.py::build_card` 的 `budget` 透传），随后 `task.cancel()` 停掉这次 Turn（否则 worker 会一直跑下去，`_terminate_run` 的幂等守卫只是让后续超限事件变成无操作的重复广播，并不会真的让 worker 停下来）——这一步刻意复刻 N07 看门狗"先 `_terminate_run` 完整落地、再 cancel"或"先 cancel 再 terminate"的既有机制（此处选择前者：不像 N07 需要在两个协程之间抢时间，这里只有一个调用方，没有理由复刻 N07 因为竞态不得不采用的顺序）。
  - ~~**已知范围缺口（写清楚，不是静默留白）**：时长检查只在 `_handle_tool_call_start`……~~ —— **已由控制者裁定 R-N8（第 6 轮，2026-09-20）接线，见 §4.4**：时长检查已整体挪出 `_handle_tool_call_start`，改成 `_run_budget_watchdog_loop` 常驻轮询。原文保留供审计：时长检查曾经只在 `_handle_tool_call_start`（每次工具调用开始）触发——一个完全不调用任何工具、纯文本生成却跑满 2 小时的 Turn，没有独立的定时器会在没有工具调用事件的情况下主动检查它。
  - **Agent 级放宽未实现**：R-N5 原文"Agent 配置可放宽（`model_pref_json` 之外新增 `limits`，可选，不做也行）"——本轮选择不做：`agents.model_pref_json` 之外新增一个 `limits` 列/JSON 字段需要一次新的迁移，且 `_run_turn` 已经要读 Agent 的 `model_pref`（`queries.get_agent_model_pref`），加一次 `limits` 读取本身不难，但"迁移 + 合并规则（Agent 级/Session 级/全局哪个优先）"这类新契约决策超出 R-N5"可选"授权的最小范围——settings.json 的全局/项目两级已经能覆盖"调整默认值"这个真实需求（改一次 settings.json 立刻对新 Turn 生效），Agent 级覆盖留给真正需要"这个 Agent 允许跑得比默认更久"的场景出现时再做。

### 4.4 第 6 轮契约变更（controller ruling R-N7/R-N8/R-N9/R-N10，2026-09-20）

- **R-N7：预算终止必须真的停住 agent**：`_terminate_run_for_exceeded_budget`（R-N5 新增，`_handle_tool_call_start`/`_check_run_durations` 共用）此前只 `task.cancel()` 本地 `_run_turn` 的 asyncio 任务——这只让该协程放弃等待 `worker.client.prompt(...)` 的返回，worker 子进程里真实的 agent 循环毫不知情，照常继续调工具、消耗预算。改法与用户终止（`stop()`）走同一条路：`worker.client.cancel(worker.acp_session_id)`（真实 ACP `session/cancel` 通知）**先**发给 worker，**再** `_terminate_run(kind="budget", ...)`（写库 + 广播，且同步设好 `ctx_turn.terminated_kind`），**最后**才 `task.cancel()` 本地任务。`cancel()` 本身失败（`AcpProtocolError`/`AcpError`，worker 已死/超时）记日志后继续终止流程，不阻塞——与 `stop()` 现有的 `except` 处理同形状。
- **R-N8：时长上限改成常驻轮询**：`_handle_tool_call_start` 不再检查时长（原地保留 Step 数检查）——`SessionService` 新增后台任务 `_run_budget_watchdog_loop`（`startup()` 起、`shutdown()` cancel+await，与 `replay/retention.py::run_sweep_loop` 同一形状），每 `_BUDGET_WATCHDOG_INTERVAL_S`（15s）调用一次 `_check_run_durations()`：遍历 `_active_turns`，对任一 `now - ctx_turn.started_at > ctx_turn.max_run_duration_s` 的 Turn 走 R-N7 同一条终止路径。时钟可注入：`SessionService.__init__` 新增 `clock: Callable[[], float] = time.monotonic` 参数（`self._clock`），`_run_turn` 用它记录 `started_at`，`_check_run_durations` 用它比较——测试构造 `SessionService(..., clock=fake_clock)` 即可确定性驱动，不需要 monkeypatch 真实 `time.monotonic`（那会连累 `workers/manager.py::_reap_idle_loop` 自己的调度，round-5 报告已记录过这个坑）。
- **R-N9：挂起状态落库**：`sessions` 表新增 `queue_suspended_reason TEXT NULL`（迁移 `008_queue_suspended_reason.sql`）——`_terminate_run` 在设置 `ctx_turn.terminated_kind` 的同一处，额外 `await` 一次 `queries.set_queue_suspended_reason(db, session_id, effective_kind)`（三类终止都写，与 `_advance_queue` 现有的"是否挂起"判断口径一致，不额外判断队列是否为空——空队列的展示层已经在 `QueuePanel.tsx` 里靠 `items.length===0` 挡掉，不需要在这里重复判断）；`_run_turn` 起始处清空它（`None`，覆盖 send()/`retry()`/`queue_resume()`/`_advance_queue` 自动续跑这几条都会走到 `_run_turn` 的路径，不需要单独碰 `send()`）；`queue_resume()`/`retry(action="abandon")` 各自在自己已有的 `suspended:false` 广播旁再显式清一次（比等 `_run_turn` 的任务真正被调度更早落地，给这两个 RPC 自己的同步响应一个立即一致的读)。`session.get` 的返回（`queries.get_session` 本来就是 `SELECT *`）因此免费带上这个字段；**`session.queue`（仅此一个，`queue_remove`/`queue_reorder` 未变）响应形状从裸 `QueueItem[]` 改成 `{items, suspended, reason}`**——00-foundation.md §4.1 已同步拆行。渲染端：`domain/types.ts` 的 `Session` 加可选 `queue_suspended_reason`、新增 `SessionQueueResponse` 类型；`chatStore.ts` 的 `bindSession()` 不再无条件把 `queueSuspendedReason` 重置成 `null` 再等一个未来才会到达的广播——`session.get`/`session.queue` 两次调用各自把持久化的值写回 state，会话切走切回（甚至 daemon 重启后渲染端重连）也能重建"已暂停" + "继续"按钮。
- **R-N10（非独立评审意见，控制者随裁定一并处理）**：R-N5 的 Agent 级 `limits` 放宽维持不做（settings.json 全局/项目两级已够，理由同 round-5 报告）。N07 看门狗（`_on_worker_crash`，本轮起正式在 N 的授权改动点内）自身 `task.cancel()`/`_terminate_run()` 的调用顺序，按 R-N7 同一顺序整理：`task = self._turn_tasks.get(session_id)` 的**捕获**位置不变（仍紧跟 `current_run` 状态读取、零 `await`，保证抓到的是真正卡住的任务，不是 R-N4 round 重排引入过又被 round-N2 修复的那种身份错位），但**实际 `.cancel()` 调用**挪到 `_finalize_streamed_messages`/`_terminate_run` 之后——收口 §4.3 记录的"N07 窄竞态"：此前 `_terminate_run` 还没来得及同步设置 `ctx_turn.terminated_kind` 时任务就被 cancel，若被取消的 `_run_turn` 恰好在此期间抢先跑到 `_advance_queue`，会把这次 worker_crash 误判成"没有终止"而正常续跑队列；现在 `terminated_kind` 保证在 `task.cancel()` 之前落地，`_advance_queue` 不会再读到这个窗口。



- **目录审计**：写一个测试对照 PRD 10.2 的用户级与项目级目录树，`paths.py` 每个访问器都有对应项，多余/缺失即失败。
- **真删（G20）**：`maintenance.delete_session(id)` / `delete_project(id)` / `delete_run(id)`：事务内删 SQLite 行（级联 turns/messages/runs/steps/permission_decisions/queue_items），再删 `runs/<id>/` payload、`projects/<id>/attachments`，最后 `wal_checkpoint(TRUNCATE)`（busy → 重试 → 抛错，按 spike 03 契约）；记忆向量分片留钩子（FR18 P1）。RPC `session.delete`、`project.delete`（已有，改为调 maintenance）、`run.delete`（新增到 v0 表）。「导出后删除」：`session.export {id} -> 路径`（JSON），删除前可选。
- **凭据**：`vault.py` 原子写补 `fsync`；启动时 Key 脱敏自检——跑一遍所有日志文件与最近 100 条 RPC 响应样本，grep 已配置 Key 的任意 8 字节子串，命中即 `daemon.error`（G03 的运行时守卫，不只靠测试）。
- **备份轮转**：迁移备份 `jones.db.bak-*` 保留最近 5 份；`logs/` 滚动 7 天；`cache/` 一键清空 RPC `daemon.clear_cache`。
- **迁移（G19）**：测试：起 daemon 于 `JONES_HOME=A`，写入会话/Agent/Skill/记忆占位，停；整目录复制到 `B`，以 `JONES_HOME=B` 启动，断言会话历史/Agent/Skill 可读，`secrets/` 因绑定密钥链而需要重录（用 `JONES_VAULT_KEY` 模拟不同机器：不同 key 解密失败必须是**显式** `vault_key_mismatch` 错误 + 提示重录，不是崩溃）。
- **重启不重放**再验一次：`queue_items` pending 在重启后仍 pending（A 已有测试，这里做端到端：真实起停 daemon）。

### 5.1 第 3 轮评审后追加的契约修订（round-3，controller ruling R-O2/R-O3，2026-09-20）

- **脱敏自检改为增量、独立 executor（R-O2）**：§5 原文「跑一遍所有日志文件」现改为「只扫每个日志文件自上次扫描以来新追加的字节」——`store/maintenance.py::_read_logs_incremental` 按已解析路径把每个文件的扫描偏移持久化到 `runtime/redaction_scan_state.json`（跨重启存活，不只是同进程内多轮循环之间）。「最近 100 条 RPC 响应样本」改为「最近 32 条，每条截至 8KB」（`rpc/server.py::RECENT_RESPONSES_MAXLEN`/`RECENT_RESPONSE_SAMPLE_MAX_CHARS`）——总量从 100×4KB=400KB 降到 32×8KB=256KB，单条覆盖面反而更大。整个扫描（Key 解密、日志/DB/payload 读取、子串扫描本身）现在跑在一个独立的单线程 executor（`_REDACTION_EXECUTOR`）上，既不占事件循环也不占 `store/db.py` 的专用 DB 线程——DB 半区改为在这个 executor 线程上开一个短生命周期的只读连接（`_recent_db_texts_readonly`），不再复用共享的 `check_same_thread=True` 连接。`startup_key_redaction_self_check`/`run_redaction_self_check_loop` 的签名相应变化：`conn: sqlite3.Connection` 参数换成 `db_path: Path` + 新增 `runtime_dir: Path`。
- **脱敏自检按来源分别设扫描上限，不再对拼接后的整体 haystack 一刀切尾部（round-3 review 意见 1）**：`MAX_HAYSTACK_CHARS`（单一全局上限，join 之后截断）被 `MAX_HAYSTACK_CHARS_PER_SOURCE`（logs/db/run_payloads/rpc_responses 四个来源各自 2MB，join 之前分别截断）取代——旧写法下，`_recent_payload_texts` 没有自己的聚合字节上限（一个 Run 可能有几十到上百个 payload 文件），一旦联合后的字符串超过全局上限，被整段挤掉的恰好是排在列表最前的日志文件，且没有任何告警。现在每个来源独立触顶、独立记日志（`_cap_haystack_source`），互不挤占。
- **`logs/` 滚动改为完全交给 `daemon.log` 自身的 size-based rotation（R-O3）**：`store/maintenance.py::rotate_logs`/`run_log_rotation_loop`（旧的 mtime 扫描 sweep）整体删除——`logging.py::configure_logging` 的 `daemon.log` handler 从 `TimedRotatingFileHandler`（午夜切割/7 天 `backupCount`，纯时间预算）改为 `RotatingFileHandler`（`DAEMON_LOG_MAX_BYTES=10MB` × `DAEMON_LOG_BACKUP_COUNT=7`，size-based），且 stderr 输出改为只在 `isatty()` 为真时开启——launchd 托管（非交互终端）下不再重复写 stderr，`logs_dir` 下除 `daemon.log` 外不再有任何东西需要清理，原 sweep 存在的理由（daemon.out.log/daemon.err.log 永远删不掉）随之消失。`__main__.py` 不再启动/取消这个循环。

## 6. 全体

- 性能：L 单定时器无轮询（断言空闲 CPU 无唤醒）；N 分类器纯计算；O 真删在 DB 线程、payload 删除不阻塞事件循环。
- 诚实失败：任何删除半途失败 → 记录部分完成状态并 `daemon.error`，不假装删完。
