# N05 — 用户数据被发送到未配置的远端

**状态：待执行**——与 G16 是同一份抓包证据，不重复建模板。

> PRD 12.2: "N05 | 用户数据（对话、记忆、凭据、文件内容）被发送到用户未配置、
> 未显式启用的任何远端 | 对应原则 5.1、11.3"

见 `docs/acceptance/v1.0/G16.md`：同一次抓包，G16 核对"有没有多余的出站请求"，
N05 核对"每一个出站请求里有没有不该被发出去的用户数据"（多了一层内容检查：
确认发往模型 API 的请求体里只有该次 Turn 正常应该发送的内容，没有夹带其它
Session/凭据/无关文件）。
